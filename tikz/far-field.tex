% Requires:
% \providecommand{\gls}[1]{#1}
\begin{tikzpicture}
  \node[circle,draw,fill=red,thick,minimum size=0.3cm,label=above:Source] (source) at (0,0) {};

  \foreach \r in {0.25,0.5,0.75,1.0,1.25,1.5,1.75,2.0} {
    \draw[thick,blue] (source) ++(225:\r) arc (225:315:\r);
  }

  \node[align=center,font=\footnotesize] (nf) at (0, -2.5) {\textbf{Spherical wavefronts}\\(\gls{near field})};

  \draw[dotted,thick] (nf) -- ++(0,-1) coordinate (dots);

  \foreach \r in {0.25,0.5,0.75,1.0} {
    \path (nf.west |- dots) -- ++(0,{-\r}) coordinate (c);
    \draw[thick,blue] (c) -- (nf.east |- c);
  }

  \node[align=center,font=\footnotesize] at (0, -5.0) {\textbf{Planar wavefronts}\\(\gls{far field})};
\end{tikzpicture}
